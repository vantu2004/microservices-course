spring:
  application:
    name: configserver
  profiles:
    # active: native
    active: git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/vantu2004/easybytes-config.git"
          default-label: master
          timeout: 5
          clone-on-start: true
          force-pull: true
        # native:
          # search-locations: "classpath:/config"
          # search-locations: "file:///C://Users//tulev//OneDrive//Documents//bin//config"

server:
  port: 8071

encrypt:
  key: "Ux4B(t,6X]shYE[9"
